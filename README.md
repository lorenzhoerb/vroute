# vrouter

**vrouter** is a lightweight virtual router written in Go.  
It simulates link-state routing protocols, allowing you to build network topologies, exchange neighbor information, and compute shortest-path routing tables.
